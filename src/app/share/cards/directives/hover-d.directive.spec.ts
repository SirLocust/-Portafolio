import { HoverDDirective } from './hover-d.directive';

describe('HoverDDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverDDirective();
    expect(directive).toBeTruthy();
  });
});
